<!DOCTYPE html>
<html lang="pt-br" style="width: 100vw; height: 100vh;">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <title>Acumuladores</title>
</head>
<body class="container h-100 w-50 d-flex flex-column justify-content-center bg-body-secondary">
    <div class="border border-2 border-primary rounded-4 p-5 bg-white shadow">
        <h2 class="text-center">Comparar acumuladores</h2>
        <form action="/php/acumuladores/comparar" method="post">
            <div class="mb-3 fs-5">
                <label for="empresaBase" class="form-label">Empresa base</label>
                <input 
                    list="empresas" 
                    class="form-control" 
                    id="empresaBase"
                    name="empresaBase" 
                    autocomplete="off"
                    required
                >
                <datalist id="empresas">
                    {% for empresa in empresas %}
                        <option value="{{empresa}}"></option>
                    {% endfor %}
                </datalist>
            </div>
            <div class="mb-3 fs-5">
                <label for="empresaComparacao" class="form-label">Empresa comparação</label>
                <input 
                    list="empresas" 
                    class="form-control" 
                    id="empresaComparacao" 
                    name="empresaComparacao"
                    autocomplete="off"
                    required
                >
            </div>
            <div class="form-group mb-4 fs-5">
                <label>Competência dos acumuladores</label>
                <input type="month" name="date" value="{{'now'|date('Y-m')}}" class="form-control" required>
            </div>
            <div class="d-flex justify-content-center">
                <button type="submit" class="btn btn-primary fs-5">Comparar</button>
            </div>
        </form>
    </div>
</body>
</html>