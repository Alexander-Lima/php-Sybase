<!DOCTYPE html>
<html lang="pt-br" style="width: 100vw; height: 100vh;">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <title>Acumuladores</title>
</head>

<body class="container h-100 w-75 d-flex flex-column justify-content-center bg-body-secondary">
    <div class="border h-100 border-2 border-primary rounded-4 m-3 bg-white shadow overflow-hidden">
        <h1 class="text-center bg-primary text-light p-2">Erros encontrados</h1>
        <div class="overflow-y-auto h-100 ps-3 pe-3">
            {% if errors|length == 0 %}
                <div class="d-flex justify-content-center align-items-center h-100">
                    <h2 class="fs-4">Nenhum erro encontrado.</h2>
                </div>
            {% endif %}
            {% for error in errors %}
                <div class="card m-2">
                    <div class="card-body">
                        <h4 class="card-title text-center">Acumulador {{error.codAcumulador}}</h4>

                        {% for item in error.list %}
                            <p class="card-text">
                                <strong>{{item.name}}</strong>: {{item.description[0]}}
                                <span class="text-black"> ➤ </span> {{item.description[1]}}
                            </p>
                        {% endfor %}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</body>

</html>